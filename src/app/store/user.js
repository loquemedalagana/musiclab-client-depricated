import {createSlice} from '@reduxjs/toolkit';

const slice = createSlice({
    name: 'user',
    initialState: {
        username: 'JeonInhyuk',

    },
    reducers: {

    },
});

export default slice;